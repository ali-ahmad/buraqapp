<h1>Roles#index</h1>
<p>Find me in app/views/roles/index.html.erb</p>

<%= simple_form_for(@role) do |f|
	f.input :title
	f.simple_fields_for(@role.rights) do |field|
		@role.rights.each do |key, value|
			field.input :key
		end
	end
end %>